{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Accept 'Karigar/Factory Name' header variants in Master Designs Excel import",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Extend Master Designs Excel header aliasing so \"Karigar/Factory Name\" (and normalized variants) maps to `karigarName`, preserving existing supported headers and showing correct values in the import preview.",
      "acceptanceCriteria": [
        "Given an Excel file where the karigar column header is exactly \"Karigar/Factory Name\", importing via the Master Designs import dialog parses rows with `entry.karigarName` set to the cell values (not \"Unassigned\" unless the cell is empty).",
        "Given an Excel file where the karigar column header is a common variant such as \"Karigar / Factory Name\" or \"Karigar-Factory Name\" (i.e., variants that normalize to the same normalized key), the parser still maps it to `karigarName`.",
        "No regression: existing supported headers such as \"Karigar Name\", \"Factory\", and \"Factory Name\" continue to be recognized.",
        "When the parsed preview is shown in the import dialog, the \"Karigar Name\" column displays the parsed values for affected files."
      ],
      "file_operations": [
        {
          "path": "frontend/src/lib/parsers/excel/parseMasterDesignsFromWorkbook.ts",
          "operation": "modify",
          "description": "Add the normalized header alias for \"Karigar/Factory Name\" (e.g., `karigar_factory_name`) to the `karigarName` column aliases so that headers like \"Karigar/Factory Name\", \"Karigar / Factory Name\", and \"Karigar-Factory Name\" are recognized and populate `entry.karigarName` (falling back to \"Unassigned\" only when the cell value is empty)."
        }
      ]
    }
  ]
}